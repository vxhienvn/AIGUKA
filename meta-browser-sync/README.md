# Meta Browser Sync

Module này để dành cho bước lấy bằng chứng từ Meta Business Suite khi Graph API không có đủ `ad_id` lịch sử.

Bản V6.1 Stable hiện ưu tiên `/lead-tracker` quét từ bảng `messages`. Nếu lead bị nhóm vào `unknown_ad`, cần dùng Browser Sync/Pancake Sync để bổ sung nguồn quảng cáo.
